% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ec_prepare_data.R
\name{ec_prepare_data}
\alias{ec_prepare_data}
\title{Prepare Protected Areas Data for ProtConn Analysis}
\usage{
ec_prepare_data(pa, ecoregion, min_area = 1, projection_crs = NULL)
}
\arguments{
\item{pa}{Either a file path to a protected areas shapefile or an sf object containing
protected areas polygons}

\item{ecoregion}{Either a file path to an ecoregions shapefile or an sf object containing
ecoregion boundaries}

\item{min_area}{Numeric. Minimum area threshold in square kilometers for protected areas
to be included in the analysis. Default is 1 km^2}

\item{projection_crs}{Coordinate reference system for area calculations. Can be a crs object
or input string for \code{\link[sf:st_crs]{sf::st_crs()}}. If NULL, uses the CRS of the ecoregions layer.
Default is NULL}
}
\value{
A list containing four elements:
\code{pas}: sf object with dissolved protected areas polygons
\code{raw_pas}: sf object with original (non-dissolved) protected areas intersected with ecoregions
\code{ecoregions}: sf object with ecoregions data
\code{ecoregion_pas}: sf object with protected areas clipped to ecoregion boundaries
}
\description{
This function prepares protected areas (PA) and ecoregion data for connectivity analysis
by processing spatial layers, filtering by minimum area, and performing spatial intersections.
The function handles both file paths and sf objects as input.
}
\examples{
library(dplyr)
# Benin - treated as single ecoregion
benin <- fiwac \%>\%
  dplyr::filter(ISO3_CODE == "BEN")

# Protected area
prot_area <- pas

# Prepare data
prepared_data <- ec_prepare_data(
  pa = prot_area,
  ecoregion = benin,
  min_area = 1
)
}
\references{
Saura, S., Bastin, L., Battistella, L., Mandrici, A., & Dubois, G. (2017).
Protected areas in the world's ecoregions: How well connected are they?
Ecological Indicators, 76, 144-158. \doi{10.1016/j.ecolind.2016.12.047}
}
